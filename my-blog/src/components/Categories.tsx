"use client";

type Props = {
  categoreies: string[];
  seleted: string;
  onClick: (category: string) => void;
};

export default function Categories({ categoreies, seleted, onClick }: Props) {
  return;
}
